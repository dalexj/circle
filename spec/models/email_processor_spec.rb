require 'spec_helper'

describe EmailProcessor do
  it 'checks to see if the user already exists' do
    # email = build(:email, :with_attachment)
    # user = EmailProcessor.new(email).process
  end
end
